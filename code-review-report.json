{
  "schema_version": "1.0",
  "scope": {
    "target": "openspec/changes/assignee-filter implementation",
    "input_type": "files",
    "focus": "full review against assignee-filter, assignee-delta, and assignee-avatars specs",
    "reviewed_at": "2026-06-24T20:57:00Z"
  },
  "summary": {
    "verdict": "block",
    "narrative": "The assignee-filter change threads assignee data through the full pipeline (GraphQL query, delta detection, poller, agent service, webview) and implements the core assignee-only selection filter. The GraphQL query, data types, and avatar rendering are solid. However, there are two correctness gaps that block merging: (1) the critical assignee filter in selectNextIssue() has no dedicated unit tests — existing tests bypass the filter entirely by omitting currentUser, and (2) finishAgent() does not trigger the required 'no more work available' popup when no assigned issues remain. Additionally, the command test simulation is missing the no_assigned_issues handler, and the delta detection drops concurrent non-assignee events.",
    "counts": {
      "critical": 0,
      "high": 2,
      "medium": 4,
      "low": 3,
      "info": 1
    }
  },
  "dimensions": [
    {
      "category": "correctness",
      "status": "reviewed",
      "note": "5 findings — assignee filter untested, finishAgent popup missing, test simulation drift, delta event masking, stories missing"
    },
    {
      "category": "architecture",
      "status": "reviewed",
      "note": "1 finding — finishAgent return contract mismatch"
    },
    {
      "category": "performance",
      "status": "reviewed",
      "note": "1 finding — JSON stringify for assignee comparison"
    },
    {
      "category": "security",
      "status": "reviewed",
      "note": "Clean — avatarUrl from GitHub CDN is safe; no innerHTML; IPC validation intact"
    },
    {
      "category": "dependency-cve",
      "status": "not-reviewed",
      "note": "No CVE scanner available in environment; no new dependencies introduced by this change"
    }
  ],
  "cve_scan": {
    "method": "none",
    "complete": false,
    "ecosystems": [],
    "packages_checked": 0,
    "notes": "No new third-party dependencies introduced by this change. Existing lockfile (package-lock.json) unchanged. No scanner available in current environment."
  },
  "findings": [
    {
      "id": "CR-001",
      "category": "correctness",
      "severity": "high",
      "confidence": "high",
      "disposition": "blocker",
      "title": "Assignee filter in selectNextIssue() has no dedicated unit tests",
      "detail": "The core feature — filtering board items to only those assigned to the current user — is exercised by zero test cases. All existing tests in agent.selectNextIssue.test.ts and agent.startAgent.test.ts pass items with assignees: [] and never call setCurrentUser(). Because the filter at agent.ts:144-152 is guarded by `if (this.currentUser)`, the filter body is never entered during tests. This means the primary requirement of the assignee-filter spec is completely untested. If the filter logic regresses (e.g., wrong comparison, off-by-one), no test will catch it.",
      "recommendation": "Create src/test/services/agent.selectNextIssue.assigneeFilter.test.ts with cases: (a) user assigned to one issue — returns that issue, (b) user assigned to none — returns null, (c) mixed assignees — returns only user's issues, (d) unassigned issue never selected. Each test must call agent.setCurrentUser('alice') before setBoardState().",
      "location": {
        "file": "src/services/agent.ts",
        "lines": [144, 152]
      },
      "snippet": "    if (this.currentUser) {\n      const before = eligible.length;\n      eligible = eligible.filter(\n        (item) => item.assignees?.some((a) => a.login === this.currentUser)\n      );\n      logger.info(`[AgentService.selectNextIssue] Filtered ${before} -> ${eligible.length} items assigned to ${this.currentUser}`);\n    } else {\n      logger.warn('[AgentService.selectNextIssue] No currentUser set — skipping assignee filter');\n    }",
      "references": [
        "openspec/changes/assignee-filter/specs/assignee-filter/spec.md",
        "openspec/changes/assignee-filter/tasks.md task 4.6"
      ]
    },
    {
      "id": "CR-002",
      "category": "correctness",
      "severity": "high",
      "confidence": "high",
      "disposition": "blocker",
      "title": "finishAgent() does not show 'no more work available' popup per spec",
      "detail": "Spec requirement (assignee-filter/spec.md, lines 44-53): 'When the agent finishes work and no assigned issues remain, the extension SHALL display a popup informing the user.' The current finishAgent() at agent.ts:290-319 logs 'No next issue available' but does not return a reason or trigger any popup. The popup logic lives in the command handler (extension.ts:129-156), but finishAgent() calls startAgent() internally — there is no path back to the command handler to display the message. The spec explicitly requires this popup for the auto-trigger flow after agent completion.",
      "recommendation": "Have finishAgent() return a result indicating whether no assigned issues remain, and have the caller (claudeSpawner or claudeTrigger callback chain) display the popup. Alternatively, add a vscode.window.showInformationMessage call directly in finishAgent() when next is null and currentUser is set. Update tasks.md task 5.4 accordingly.",
      "location": {
        "file": "src/services/agent.ts",
        "lines": [316, 318]
      },
      "snippet": "    } else {\n      logger.info('[AgentService.finishAgent] No next issue available');\n    }",
      "references": [
        "openspec/changes/assignee-filter/specs/assignee-filter/spec.md lines 44-53",
        "openspec/changes/assignee-filter/tasks.md task 5.4"
      ]
    },
    {
      "id": "CR-003",
      "category": "correctness",
      "severity": "medium",
      "confidence": "high",
      "disposition": "should-fix",
      "title": "Test simulation in startAgent.test.ts missing no_assigned_issues handler",
      "detail": "The simulateHandleStartAgent() function in src/test/commands/startAgent.test.ts:35-63 handles reasons: busy, empty, auto_move_failed — but not no_assigned_issues. The actual command handler in extension.ts:142-143 handles it with showWarningMessage. This means the test simulation diverges from production behavior. If someone refactors the popup messages, the test won't catch the regression for this specific reason.",
      "recommendation": "Add an else-if branch for result.reason === 'no_assigned_issues' in simulateHandleStartAgent() that calls vscode.window.showWarningMessage with the expected message. Add a test case that sets currentUser, provides no assigned issues, and verifies the warning message.",
      "location": {
        "file": "src/test/commands/startAgent.test.ts",
        "lines": [35, 63]
      },
      "snippet": "    } else if (result.reason === 'empty') {\n      vscode.window.showInformationMessage('No issues available for AI agent');\n    } else if (result.reason === 'auto_move_failed') {",
      "references": [
        "src/extension.ts:142-143"
      ]
    },
    {
      "id": "CR-004",
      "category": "correctness",
      "severity": "medium",
      "confidence": "high",
      "disposition": "should-fix",
      "title": "Delta detection masks concurrent non-assignee events",
      "detail": "In detectDeltas() at delta.ts:48-80, the if-else chain means only ONE event type is emitted per item per poll. If an item's status AND assignees change simultaneously, only the status change (item_moved) is emitted — the assignee change is silently dropped. Similarly, if assignees AND labels change, only item_assigned fires and the label change is lost. This is a design trade-off but means the system under-reports changes during complex edits.",
      "recommendation": "Either (a) emit multiple events per item by replacing the if-else chain with independent if checks, or (b) document this as intentional in the delta.ts header and accept it as a known limitation. Option (a) is preferred for completeness.",
      "location": {
        "file": "src/services/delta.ts",
        "lines": [48, 80]
      },
      "snippet": "    } else if (last.status !== status) {\n      // Status changed (item moved)\n      ...\n    } else if (JSON.stringify(last.assignees.map((a) => a.login).sort()) !== JSON.stringify(assignees.map((a) => a.login).sort())) {\n      // Assignees changed\n      ...\n    } else if (last.title !== title || JSON.stringify(last.labels.sort()) !== JSON.stringify(labels.sort())) {",
      "references": [
        "openspec/changes/assignee-filter/specs/assignee-delta/spec.md"
      ]
    },
    {
      "id": "CR-005",
      "category": "correctness",
      "severity": "medium",
      "confidence": "high",
      "disposition": "should-fix",
      "title": "IssueCard stories missing assignee avatar example",
      "detail": "Task 6.4 in tasks.md requires updating IssueCard.stories.tsx with assignee examples. The current file (171 lines) has no story that includes assignees data. The baseItem at line 37-47 has no assignees field. Without a Storybook story showing the avatar circles, there is no visual regression test for the avatar rendering.",
      "recommendation": "Add a story (e.g., export const WithAssignees) that includes assignees: [{ login: 'alice', avatarUrl: 'https://avatars.githubusercontent.com/u/1?v=4' }, { login: 'bob', avatarUrl: 'https://avatars.githubusercontent.com/u/2?v=4' }] to verify the avatar circle rendering in Storybook.",
      "location": {
        "file": "webview-ui/src/components/IssueCard.stories.tsx",
        "lines": [37, 47]
      },
      "references": [
        "openspec/changes/assignee-filter/tasks.md task 6.4"
      ]
    },
    {
      "id": "ARCH-001",
      "category": "architecture",
      "severity": "medium",
      "confidence": "high",
      "disposition": "should-fix",
      "title": "finishAgent() void return prevents caller from reacting to empty state",
      "detail": "finishAgent() returns Promise<void>, but the caller (claudeSpawner triggerCallback) needs to know whether there are no more assigned issues to display the popup. Currently the popup logic only exists in the command handler (extension.ts), but finishAgent() is called from the agent completion callback chain — a different code path that never reaches the command handler. This architectural gap means the 'no more work' popup can never actually fire.",
      "recommendation": "Change finishAgent() to return Promise<{ reason?: 'no_assigned_issues' | 'none' }> so the caller can branch on the result. Or add an onIdle callback to AgentService that fires when no work remains.",
      "location": {
        "file": "src/services/agent.ts",
        "line": 290
      },
      "snippet": "  public async finishAgent(issueId: string): Promise<void> {",
      "references": [
        "src/services/agent.ts:290-319",
        "openspec/changes/assignee-filter/tasks.md task 5.4"
      ]
    },
    {
      "id": "PERF-001",
      "category": "performance",
      "severity": "low",
      "confidence": "medium",
      "disposition": "consider",
      "title": "JSON.stringify for assignee comparison is O(n log n) per item per poll",
      "detail": "delta.ts:64 uses JSON.stringify(last.assignees.map(a => a.login).sort()) for comparison. For boards with many items, this allocates sorted arrays and serializes them on every poll. With first:5 assignees and typical board sizes (50-200 items), this is negligible — but the pattern is fragile if assignee limits increase.",
      "recommendation": "Consider a Set-based comparison: new Set(last.assignees.map(a => a.login)).size === new Set(assignees.map(a => a.login)).size && every member matches. Or pre-sort during state extraction. Not urgent given current scale.",
      "location": {
        "file": "src/services/delta.ts",
        "line": 64
      },
      "snippet": "    } else if (JSON.stringify(last.assignees.map((a) => a.login).sort()) !== JSON.stringify(assignees.map((a) => a.login).sort())) {",
      "references": []
    },
    {
      "id": "LOW-001",
      "category": "correctness",
      "severity": "low",
      "confidence": "high",
      "disposition": "nit",
      "title": "Integration test registerStartAgentCommand missing no_assigned_issues handler",
      "detail": "src/test/integration/startAgent.integration.test.ts:58-88 duplicates the command handler but also lacks the no_assigned_issues branch. Same issue as CR-003 but in the integration test file.",
      "recommendation": "Add the no_assigned_issues handler to the integration test's registerStartAgentCommand, mirroring extension.ts:142-143.",
      "location": {
        "file": "src/test/integration/startAgent.integration.test.ts",
        "lines": [58, 88]
      },
      "references": [
        "src/extension.ts:142-143"
      ]
    },
    {
      "id": "LOW-002",
      "category": "architecture",
      "severity": "low",
      "confidence": "high",
      "disposition": "nit",
      "title": "Avatar URL not validated before rendering in IssueCard",
      "detail": "IssueCard.tsx:98 renders src={assignee.avatarUrl} directly. GitHub's GraphQL API returns trusted avatar URLs (avatars.githubusercontent.com), so this is not a security risk in practice. However, if the data source ever changes or test data includes malformed URLs, the img tag will fail silently.",
      "recommendation": "Add a fallback: onError={() => imgRef.current.src = '/default-avatar.png'} or validate the URL starts with https://avatars.githubusercontent.com. Low priority given the data source is GitHub's own API.",
      "location": {
        "file": "webview-ui/src/components/IssueCard.tsx",
        "lines": [96, 102]
      },
      "snippet": "            <img\n              key={assignee.login}\n              src={assignee.avatarUrl}\n              alt={assignee.login}\n              title={assignee.login}\n              className=\"w-6 h-6 rounded-full border border-vscode-editor-background\"\n            />",
      "references": []
    },
    {
      "id": "PRAISE-001",
      "category": "architecture",
      "severity": "info",
      "confidence": "high",
      "disposition": "praise",
      "title": "Clean thread of assignee data through entire pipeline",
      "detail": "The assignee data flows cleanly from CONTENT_FRAGMENT through IssueContent/PullRequestContent types, extractAssignees(), BoardItemState, PrioritizerItem, and into the webview IssueCard. Each layer has proper TypeScript typing. The GraphQL fragment approach (Decision 1 in design.md) correctly avoids N+1 queries. The avatarUrl passthrough (Decision 5) eliminates a separate API call. Well-architected data flow.",
      "recommendation": "None — this is a positive pattern.",
      "location": {
        "file": "src/services/graphql.queries.ts",
        "lines": [19, 21]
      },
      "references": [
        "openspec/changes/assignee-filter/design.md"
      ]
    }
  ]
}
